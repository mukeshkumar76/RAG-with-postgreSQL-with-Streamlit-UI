# AI Hybrid RAG Agent

## Setup
1. `python -m venv venv`
2. `source venv/bin/activate`
3. `pip install -r requirements.txt`
4. Setup PostgreSQL and run `databases/schema.sql`.
5. Run `python mcp_server.py`.
6. Run `streamlit run user_interface.py`.